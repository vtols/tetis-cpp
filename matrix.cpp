#include "matrix.h"

/*template <class T>
Matrix<T>::Matrix() {h = 0; w = 0;}*/

/*template <class T>
Matrix<T>::Matrix(bool init, int lh, int lw, ...) {
    w = lw;
    h = lh;
    data.fill(QVector<T>(w), h);
    if (init) {
        va_list vl;
        va_start(vl, lw);
        for (size_t i = 0; i < h; i++)
            for (size_t j = 0; j < w; j++)
                data[i][j] = va_arg(vl, T);
        va_end(vl);
    }
}*/

/*template <class T>
T& Matrix<T>::operator () (int i, int j) {
    return data[i][j];
}*/

/*
template <class T>
int Matrix<T>::height() {
    return h;
}

template <class T>
int Matrix<T>::width() {
    return w;
}*/

